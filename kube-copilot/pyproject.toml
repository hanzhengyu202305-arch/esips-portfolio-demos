[project]
name = "kube-copilot-demo"
version = "0.2.0"
description = "Kubernetes generation and structural policy validation demo"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0,<7",
]

[tool.setuptools.packages.find]
include = ["kube_copilot*"]
