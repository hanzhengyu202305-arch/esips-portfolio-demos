name: validate-ore-api
on:
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t ore-api:ci .
      - run: kubectl apply --dry-run=client -f k8s/
